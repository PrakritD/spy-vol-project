name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install (lean v2 scientific stack — no licensed data needed)
        run: |
          python -m pip install --upgrade pip
          pip install numpy pandas pyarrow scipy scikit-learn matplotlib pytest ruff
      - name: Lint
        run: ruff check analysis tests
      - name: Test (data-free; the no-lookahead gate runs on synthetic panels)
        run: pytest -q
